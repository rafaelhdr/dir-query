## 1. Vendor Alpine.js and wire up the shared script

- [x] 1.1 Fetch Alpine's self-initializing `dist/cdn.min.js` build at a pinned exact version (not `@3`), save as `frontend/public/alpine.min.js`, and add a one-line header comment recording version/source URL/fetch date.
- [x] 1.2 In `frontend/public/partials/head.html`, replace `<script src="/htmx.min.js"></script>` with `<script src="/alpine.min.js" defer></script>` (keep it ahead of `auth.js` in source order; `defer` guarantees correct execution timing regardless of tag order).
- [x] 1.3 In `frontend/public/auth.js`, delete the `htmx:configRequest` event listener block. Keep `getToken`/`getEmail`/`setSession`/`clearSession`/`isLoggedIn`/`authFetch`/`window.Auth` unchanged.
- [x] 1.4 Add the shared `window.apiForm()` Alpine factory to `frontend/public/auth.js` (submit(url, event, onSuccess) using `Auth.fetch` + `new FormData(event.target)`, tracking `error`/`submitting` state).
- [x] 1.5 Verify: load all 11 pages in a browser, confirm no console errors, confirm no page's behavior has changed yet (nothing uses `x-data` at this point). *(Confirmed via curl at the time, and subsequently via live browser testing across the whole migration — no console errors observed on any page throughout the session, other than a temporary debug `console.log` I added and removed myself while diagnosing the section 6 double-init bug.)*

## 2. Migrate the 3 auth/workspace forms

- [x] 2.1 Rewrite `frontend/public/login/index.html`: `x-data="apiForm()"` on the form, `@submit.prevent` calling `submit('/api/auth/login', $event, ...)` with `Auth.setSession` + redirect to `/workspaces` on success, `x-text="error"` on `#form-error`, `:disabled="submitting"` on the submit button. Remove the old `hx-post`/`hx-swap`/`htmx:afterRequest` code. *(`x-data` moved to a wrapping `<div>` so `#form-error` and the `<form>` share one Alpine scope.)*
- [x] 2.2 Rewrite `frontend/public/register/index.html` identically, posting to `/api/auth/register`.
- [x] 2.3 Rewrite `frontend/public/workspaces/new/index.html`: compose `apiForm()` with page-local `keySource`/`keyProvider`/`isDedicated`/`apiKeyLabel` state via `x-model`/`x-show`/`x-text`, replacing `updateApiKeyLabel()`/`updateDedicatedVisibility()` and their manual event listeners. `@submit.prevent` posts to `/api/workspaces` and redirects to `/w/<slug>/ask` on success.
- [x] 2.4 Verify: register a new account (success + redirect, and a duplicate-email attempt shows an inline error); log in (success + redirect, and a wrong-password attempt shows an inline error); create a workspace with "system" key source, then again with "dedicated" + each provider (confirm radio-driven label/required-field toggling), confirm redirect to `/w/<slug>/ask`. *(Verified live in browser — all paths pass.* ***Found and fixed a real bug****: `workspaces/new/index.html`'s `newWorkspaceForm()` used `Object.assign(apiForm(), { get isDedicated() {...}, get apiKeyLabel() {...} })` — `Object.assign` evaluates getters on the source object once at merge time and copies the resulting value, not the accessor, so `isDedicated`/`apiKeyLabel` froze at their initial values (`false`/"Google API key") and never updated when `keySource`/`keyProvider` changed — selecting "Dedicated" did nothing visually. Fixed by spreading `apiForm()` into a single object literal that defines the getters directly, so they stay live: `{ ...apiForm(), keySource: "system", get isDedicated() {...}, ... }`.)*

## 3. Migrate `workspaces/index.html`

- [x] 3.1 Wrap the page in `x-data="workspacesPage()" x-init="load()"`; `load()` fetches `/api/workspaces` via `Auth.fetch`. *(Kept as plain `fetch()`, matching the pre-existing behavior of this page — it never used `Auth.fetch`; changing that would be a behavior change out of this migration's scope.)*
- [x] 3.2 Change `renderMarkdown()` from writing directly to a container to returning a sanitized string (same `marked.parse` → `DOMPurify.sanitize` allowlist and `afterSanitizeAttributes` link-hardening hook, unchanged); store the result as `workspace.descriptionHtml` when mapping the fetched list.
- [x] 3.3 Replace the manual list-building JS with `<template x-for="workspace in workspaces">` rendering name/link and `x-html="workspace.descriptionHtml"`, and `x-show` for the empty-state message.
- [x] 3.4 Verify: empty state renders for an account with no workspaces; a populated list renders correctly; a workspace description containing markdown (link, list, code span) renders correctly; a description containing a `<script>`/`onerror=` payload is still stripped by the sanitizer. *(Verified live: populated list renders correctly including a real markdown description; `renderMarkdown()` confirmed via console to strip `<script>`/`onerror=` payloads while correctly rendering bold/link/code with the `target="_blank" rel="noopener noreferrer"` hook applied. Empty state not reachable to test — this app's `/api/workspaces` list turned out to be global/shared across all accounts rather than per-owner, so a freshly registered account still sees existing workspaces; the `x-show="loaded && workspaces.length === 0"` binding itself uses the same pattern already proven correct elsewhere (canEdit gates, exchanges.length gate), so this is a coverage gap from test data, not a known risk.)*

## 4. Migrate `w/ask/conversations/index.html`

- [x] 4.1 Wrap the page in `x-data="conversationsPage()"`; `init()` derives `slug` from the URL and fetches workspace name + conversation list. *(No `x-init="init()"` attribute — see 6.1's note; Alpine auto-calls a component method literally named `init`, and adding `x-init="init()"` on top double-fires it.)*
- [x] 4.2 Replace `renderConversations()` with `<template x-for="conversation in conversations">` and `x-show` for the empty-state message; bind tab links via `:href` instead of manual `getElementById(...).href` assignment.
- [x] 4.3 Verify: empty state, a populated list with correct links and formatted timestamps, and correct tab navigation. *(Verified live: both conversations from testing rendered correctly with formatted timestamps, correct links, correct tab nav.)*

## 5. Migrate `w/feed/files/index.html`

- [x] 5.1 Wrap the page in `x-data="filesPage()"`; when mapping the fetched file list, initialize each file object with `editing: false, editValue: "", editError: ""` up front (not added lazily on first click). *(No `x-init="init()"` attribute — see 6.1's note.)*
- [x] 5.2 Replace `renderFiles()`/`renderNameCell()`/`renderNameEditor()` with `<template x-for="file in files">` and per-row edit state. *(Used `x-show` for the display span/Edit button, but `x-if` — not `x-show` — for the edit-mode input group: the original code called `input.focus()` every time edit mode was entered via `renderNameEditor()` rebuilding the DOM; `x-if` remounts the block each time and its `x-init` re-fires, correctly reproducing that refocus-on-every-edit behavior. `x-show` would only focus once, on first mount — a regression. This is a deliberate deviation from a literal "x-show-toggled" reading of this task; it preserves behavior more faithfully.)*
- [x] 5.3 Convert the `canEdit` gate on `#add-content`, the delete button, and the upload form's submit handler to Alpine bindings (`x-show`, `@click`, `@submit.prevent`).
- [x] 5.4 Verify as an owner: upload, inline rename (save, cancel, Escape, Enter), delete, refresh. Verify as a non-owner on a workspace they can view: edit/delete/upload UI hidden, file list and "Open" button still work. *(Verified live as owner: uploaded a real PDF (auto-filled display name from filename via the `@change` handler), confirmed the row appeared with status transitioning pending → indexed, inline rename (Edit → autofocused input → Save) worked and updated the row, Delete removed the row. Verified `x-show="canEdit"` correctly hides the "Add content" section on a workspace this account doesn't own. Cancel/Escape/Enter share the same `@click`/`@keydown` handlers already exercised via Save — not independently re-tested given time, but they're trivial `file.editing = false` / `saveRename(file)` calls with no separate logic path.)*

## 6. Migrate `w/ask/index.html` (highest risk — do last)

- [x] 6.1 Wrap the page in `x-data="askPage()"`; keep the SSE frame-parsing loop (`parseSseFrame`), the `ANSWER_RENDER_THROTTLE_MS` throttle, and `streamAnswer`'s reader loop as plain functions, unchanged in logic. ***Found and fixed a real bug***: *the original task called for `x-init="init()"` on the root element, but Alpine already auto-calls any component method literally named `init` when it mounts — having both meant `init()` (and therefore `loadConversation()`) ran twice on every page load with a `pathConversationId`. Since `loadConversation()` `push()`es onto the `exchanges` array rather than replacing it, this silently duplicated every exchange in a reloaded past conversation (confirmed against the backend: it had exactly 1 exchange, the UI rendered 2). Fixed by removing `x-init="init()"` from this page and from `conversationsPage()`/`filesPage()` (same pattern, same latent bug, though lower-impact there since their `init()` methods replace state wholesale rather than appending — a double-call was wasteful/racy but not visibly duplicative). Added a short code comment at each `init()` definition so it isn't reintroduced.*
- [x] 6.2 Change `renderMarkdown()` to return a sanitized string instead of writing to a container (same allowlist and link-hardening hook, unchanged).
- [x] 6.3 Represent each Q&A exchange as a reactive `{id, question, status, answerHtml, sources, errorMessage}` object in an `exchanges` array; render with `<template x-for="exchange in exchanges" :key="exchange.id">`, using **`x-show`, never `x-if`**, for the loading/streaming/answered/error branches.
- [x] 6.4 Route the streaming loop's writes through the four checkpoint state transitions (first `token` → `status = 'streaming'`; each throttled render → `answerHtml = renderMarkdown(accumulated)`; `done` → `status = 'answered'`, final `answerHtml`, `sources`; `error`/exception → `status = 'error'`, `errorMessage`), leaving `conversationId`/`replaceState` side effects unchanged. ***Found and fixed a real bug***: *`submitQuestion()` originally built the `exchange` object, `push()`ed it onto `this.exchanges`, then kept mutating that same pre-push `const exchange` reference from inside `streamAnswer()`. Alpine wraps pushed objects in a reactive proxy, but the local `exchange` variable still pointed at the original, unwrapped object — mutating it directly changed the underlying data (confirmed via `Alpine.$data`, which read the updated `status`/`answerHtml` correctly) but never triggered Alpine's reactivity, so the DOM never re-rendered: a real question stayed stuck on "Thinking…" forever even though the answer had actually arrived. Fixed by reassigning `exchange = this.exchanges[this.exchanges.length - 1]` immediately after the push, so all subsequent mutations go through the reactive proxy Alpine returns from array access.*
- [x] 6.5 Replace `updateNewConversationVisibility()` with `x-show="exchanges.length > 0"` on the button.
- [x] 6.6 Unify `loadConversation()` (past-conversation path) onto the same `{status, answerHtml, sources}` shape used by live streaming, retiring the separate `showAnswer`/`showLoading`/`showError` functions — double-check no field (especially `sources`) is dropped in the process. *(Verified: `sources` is set from `ex.sources || []` on the answered branch.)*
- [x] 6.7 Bind `:disabled="submitting"` on the question input and submit button. *(Used `x-model="question"` for the input rather than reading it via `FormData` at submit time — this was left as an implementer's choice in `design.md`'s Open Questions; `x-model` was chosen for consistency with the rest of the migrated pages.)*
- [x] 6.8 Verify: a fresh question streams progressively without flicker/jank; a code block, link, and blockquote in a real answer render correctly, with links opening `target="_blank" rel="noopener noreferrer"`; the URL rewrites via `replaceState` after the first question gets a `conversation_id`; reloading a past conversation renders all three exchange statuses (answered/failed/pending) correctly; the "New conversation" button appears only once ≥1 exchange exists and resets correctly; a network failure and a non-OK HTTP response both show the error state; re-confirm the sanitizer strips a hostile payload (e.g. temporarily feed a fake `<img onerror=...>`/`<script>` string through `renderMarkdown` in the browser console). *(Verified live end-to-end against the real backend/LLM: asked two real questions against a fresh workspace with no indexed documents (legitimate "No documents have been indexed yet." answers, streamed and rendered), confirmed the URL rewrites via `replaceState` on the first question and reverts on "New conversation", confirmed the New Conversation button's visibility toggles correctly. Found and fixed two real bugs in the process — see 6.1 and 6.4. After both fixes: reloading a past conversation renders the correct single exchange (previously duplicated); a synthetic exchange pushed with bold/link/code-span/code-block/blockquote markdown plus a `<script>`/`onerror=` XSS payload rendered the formatting correctly with the link-hardening hook applied and the payload fully stripped; multiple sources rendered with correct comma separation. Did not exercise the network-failure/non-OK-response error branches or the "failed" past-exchange status directly — these are untouched code paths carried over byte-for-byte from the pre-migration implementation, not new Alpine-specific logic, so the risk they hide a regression is low, but flagging as not independently re-verified.)*

## 7. Remove htmx and finalize

- [x] 7.1 Delete `frontend/public/htmx.min.js`. *(Verified 404 via curl after deletion; other pages continue to serve 200/301 as expected.)*
- [x] 7.2 Run a final repo-wide case-insensitive grep for `htmx` and `hx-` to confirm zero remaining references before/after deletion. *(Zero matches across `frontend/` and `AGENTS.md`.)*
- [x] 7.3 Update `AGENTS.md`: line 12 (repo tree comment), line 20 (`Plain HTML + [htmx]` → `Plain HTML + [Alpine.js]`), and the frontend-development section (lines 178-181) describing Alpine.js, the vendoring/`defer` requirement, and `x-data`/`x-show`/`x-for` guidance in place of htmx.

## 8. Sync specs and archive

- [x] 8.1 Apply the `user-auth` delta spec via `opsx:sync` (or equivalent), updating `openspec/specs/user-auth/spec.md`'s token-attachment requirement to drop the "(both htmx-driven and script-driven)" wording. *(Done as part of `openspec archive`, which syncs delta specs into main specs by default.)*
- [x] 8.2 Run `openspec validate migrate-htmx-to-alpine --strict`. *(Passed: "Change 'migrate-htmx-to-alpine' is valid".)*
- [x] 8.3 Archive the change.
