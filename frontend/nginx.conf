server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;
    absolute_redirect off;
    ssi on;

    location /partials/ {
        internal;
    }

    location ~ ^/w/[^/]+/ask/?$ {
        try_files /w/ask/index.html =404;
    }

    location ~ ^/w/[^/]+/?$ {
        try_files /w/ask/index.html =404;
    }

    location ~ ^/w/[^/]+/feed/upload/?$ {
        try_files /w/feed/upload/index.html =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }

    location /api/ {
        proxy_pass http://backend:8000/;
    }
}
